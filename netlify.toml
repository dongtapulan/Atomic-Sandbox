[build]
  # Tell Netlify to look inside 'src' for the project files
  base = "src"
  
  # The command to build your Vite project
  command = "npm run build"
  
  # Where the final files will be (Vite defaults to 'dist')
  publish = "dist"